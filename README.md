# FdF (Fil de Fer)

Wireframe 3D landscape renderer written in C for the 42 school curriculum.

Reads a grid of altitudes from a map file and draws it as a 3D wireframe projection, with events to move and adjust the view.

Build with `make`.
